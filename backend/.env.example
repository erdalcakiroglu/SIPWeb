PORT=3001
SESSION_SECRET=change-this-secret
# Veritabanı ve session dosyaları (opsiyonel; varsayılan: proje/data)
# DATA_DIR=./data
ADMIN_EMAIL=admin@sqlperformance.ai
# Düz metin (geliştirme) veya hash (üretim). Hash için: node -e "const {hashPassword}=require('./dist/lib/password');console.log(hashPassword(process.argv[1]))" 'YourPassword'
ADMIN_PASSWORD=Admin12345!
# ADMIN_PASSWORD_HASH=
# CORS: virgülle ayrılmış origin listesi (boş = tüm origin)
# CORS_ORIGIN=http://localhost:3000,https://app.example.com
# Rate limit (istek/dakika veya pencerede max)
# RATE_LIMIT_GENERAL=100
# RATE_LIMIT_AUTH=10
# RATE_LIMIT_LICENSE_SENSITIVE=20
# RATE_LIMIT_ADMIN_LOGIN=5
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=no-reply@example.com
